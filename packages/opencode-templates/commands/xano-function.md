---
description: Create a XanoScript function (reusable business logic)
agent: xano-function-writer
---

# Create XanoScript Function

$ARGUMENTS

## Task

Create a XanoScript function based on the requirements above.

Consider:

- Input parameters with proper types and validation
- Business logic with variables, conditionals, loops
- Error handling with preconditions and try_catch
- Response structure
- Unit tests if appropriate
- Save to `functions/<namespace>/<name>.xs`
