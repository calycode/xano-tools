---
description: Create a XanoScript API query (REST endpoint)
agent: xano-api-writer
---

# Create XanoScript API Endpoint

$ARGUMENTS

## Task

Create a XanoScript API query (endpoint) based on the requirements above.

Consider:

- HTTP method (GET, POST, PUT, PATCH, DELETE)
- Input validation and types
- Authentication requirements (auth = "user")
- Database operations needed
- Response structure
- Save to `apis/<api_group>/<query_name>.xs`
