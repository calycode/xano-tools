import { nodeConfigStorage } from '../node-config-storage';

const {
   ensureDirs,
   loadGlobalConfig,
   saveGlobalConfig,
   loadInstanceConfig,
   saveInstanceConfig,
   loadToken,
   saveToken,
} = nodeConfigStorage;

export {
   ensureDirs,
   loadGlobalConfig,
   saveGlobalConfig,
   loadInstanceConfig,
   saveInstanceConfig,
   loadToken,
   saveToken,
};
